group 'co.iay.leetcode'
version '1.0'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

sourceSets {
    main {
        java.srcDirs = ['src']
    }
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
